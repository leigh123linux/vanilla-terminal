
Vanilla terminal is derived from gnome-terminal 3.6.2.2.

Its goal is to provide a terminal application that isn't dependent on any particular desktop environment and that can be used as default in many of them.

